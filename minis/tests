basic

    norminette
    memory sanitize & leaks check

-g3 -fsanitize=address

built-in command

    echo

echo
echo simple
echo -n simple
echo simple text
echo $PATH
echo "$PATH"
echo '$PATH'
echo '\'
echo "\"			// syntax error
echo "\\"
echo "\n \n \n"
echo "\n \\n \\\n"
echo hi; echo hihi;
echo ;;				// syntax error
echo hi";" hihi
echo hi "   ;   " hihi
echo one; echo two; echo three;

cd

cd
cd .
cd ..
cd ~
cd /
cd no_file
cd a b c d

    pwd

pwd
pwd a
pwd a b c d 

    export && env && unset

export
export LOL	// export vs env
export LOL=lol	// export vs env
export LOL	// export vs env
unset LOL
export LOL=lala ROR=rara // export vs env
unset LOL ROR
export "HI= hi"
export "HI =hi" // syntax error, check $?

non built-in command

/bin/ls
ls
ls -al
clear
whoami

signal and ctrl-D

$> ^C
$> echo $?
1 (or 130 in ubuntu)
$> write something^C
1 (or 130 in ubuntu)
$> cat
^C
$> echo $?
130

ctrl-D without command

$> ^D
(minishell stopped)

BASH> echo $?
0

ctrl-D with command

$> write something^D
[no command error]
$> ec^Dho hello~
hello~
$> cat
^D
$> echo $?
0

$> ^\	// nothing happend
$> write something^\	// nothing happend
$> cat
^\
$> echo $?
131

pipe

$> | echo
[syntax error]
$> echo |
[syntax error]
$> echo | | echo
[syntax error]

Pipe execution test

$> sort | ls
Desktop Music Templates
^C

If there is no result of ls and waiting for stdin, it means your minishell doesn't follow real bash. Our shell must wait for child process AFTER having executed all the child process.
redirection

$> cat < >
[syntax error]

$> cat < <
[syntax error]

$> cat < Makefile > test1 > test2
cat test1
[empty]
cat test2
[Makefile content]

$> cat < no_file > test1 > test2
cat test1
[empty]
cat test2
[empty]

$> cat < no_file > test1
[file not exist error, and test1 not created]
$> echo $?
2

pipe & redirection

$> cat < Makefile | grep gcc > output
$> cat < nofile | grep gcc > output
$> echo $? 
1

Go crazy

$> exit 0 | exit 1	// without quiting shell
$> echo $?
1
$> exit 1 | exit 0	// without quiting shell
$> echo $?
0

$> super super long text...